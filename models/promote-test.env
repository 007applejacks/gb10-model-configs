# UI-imported model recipe (host-local; gitignored). Promote via Export to version it.
# Tuning knobs (spec-decode/KV/sampling) are pinned OFF — validate + tune before trusting.

# --- UI metadata ---
UI_LABEL="Promote Test"
UI_DESC=""
UI_SIZE=""
EXPERIMENTAL="0"

# --- Serve recipe ---
SERVE_MODEL="Qwen/Qwen2.5-0.5B-Instruct"
SERVED_NAME="promote-test"
SERVE_CONTAINER="swap-vllm-promote-test"
SERVE_PORT="8002"
MAX_LEN="32768"
GPU_UTIL="0.85"
QUANT="auto"
TOOLS="1"
REASONING_PARSER="qwen3"
KV_ARG=""
GENCFG_ARG=""
EXTRA_ARGS=""
SPEC_ARG=""
COMPILE_ARG=""
