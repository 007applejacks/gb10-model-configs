# UI-imported model recipe (host-local; gitignored). Promote via Export to version it.
# Tuning knobs (spec-decode/KV/sampling) are pinned OFF — validate + tune before trusting.

# --- UI metadata ---
UI_LABEL="Qwen3.8-27B"
UI_DESC="Imported from HuggingFace — recipe is provisional, validate before relying on it. Vision-language (image-text-to-text), hybrid linear/full-attention, MTP-capable. Native context 262144 confirmed from config.json text_config (importer missed it — nested, not top-level)."
UI_SIZE="27B · BF16 unquantized · vision"
UI_SPEED="~16.5 tok/s (measured 2026-08-16)"
UI_USE_CASES=""
EXPERIMENTAL="0"

# --- Serve recipe ---
SERVE_MODEL="Qwen/Qwen3.8-27B"
SERVED_NAME="qwen3.8-27b"
SERVE_CONTAINER="swap-vllm-qwen3.8-27b"
SERVE_PORT="8002"
MAX_LEN="262144"
GPU_UTIL="0.85"
QUANT="auto"
TOOLS="1"
REASONING_PARSER="qwen3"
KV_ARG=""
GENCFG_ARG=""
EXTRA_ARGS=""
SPEC_ARG=""
COMPILE_ARG=""
